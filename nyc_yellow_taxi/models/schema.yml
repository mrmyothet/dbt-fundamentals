version: 2

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null

sources:
  - name: bigquery_source
    database: ny-rides-mt # your BigQuery project
    schema: ny_rides_ds # your dataset name
    tables:
      - name: yellow_tripdata_2023-01
